# 多目标优化算法论文任务清单

## 📋 任务概览

基于当前 min_code 文件夹中的 cSA-GOA 算法，完成以下任务以支撑论文发表。

---

## 🎯 任务1：整理现有代码，确保算法完整可运行

### 任务目标
- 验证当前算法可以正常运行
- 检查所有依赖文件是否完整
- 确保贝叶斯优化 + 自适应参数调整功能正常

### 执行步骤
1. [x] 运行 `main.m`，验证算法可以正常执行
2. [x] 检查 `optimizeHyperparameters.m` 是否存在
3. [x] 验证贝叶斯优化参数保存/加载功能
4. [x] 检查所有可视化图表是否正常显示
5. [x] 修复发现的任何 bug

### 预期成果
- 算法可以完整运行（触发提前停止或达到最大FES）
- 生成 Pareto 前沿、Knee Point、所有可视化图表
- 贝叶斯优化参数正常保存

---

## 🎯 任务2：实现性能指标计算

### 任务目标
实现以下多目标性能指标：

| 指标 | 说明 | 优先级 | 无人机场景适用性 |
|------|------|--------|------------------|
| HV (Hypervolume) | 超体积，衡量Pareto前沿质量 | ⭐⭐⭐⭐⭐ | ✅ 适用 |
| IGD (Inverted Generational Distance) | 反向世代距离 | ⭐⭐⭐⭐⭐ | ✅ 适用 |
| Spread / MS | 分布性指标 | ⭐⭐⭐⭐ | ✅ 适用 |

### 物理/应用层指标

| 指标 | 说明 | 优先级 |
|------|------|--------|
| 高优先级覆盖率 | 核心业务指标 | ⭐⭐⭐⭐⭐ |
| 全局覆盖率 | 系统吞吐量指标 | ⭐⭐⭐⭐⭐ |
| 单位覆盖能耗 | 总能耗 / 覆盖总人数 | ⭐⭐⭐⭐⭐ |
| 收敛代数 | 达到95%性能所需的FES | ⭐⭐⭐⭐ |

### 执行步骤
1. [x] 实现 `hypervolume.m` - 超体积计算
2. [x] 实现 `igd.m` - 反向世代距离
3. [x] 实现 `spread.m` - 分布性指标
4. [x] 实现 `wilcoxon_test.m` - Wilcoxon秩和检验

### 预期成果
- 所有性能指标完整实现
- 支持批量计算（多个算法、多次运行）
- 支持统计显著性检验（p < 0.05）

---



### 消融实验变体

| 变体 | 说明 | 优先级 |
|------|------|--------|
| Model 0 (Proposed) | 完全体 cSA-GOA（所有改进） | ⭐⭐⭐⭐⭐ |
| Model 1 (w/o Targeted Levy) | 去掉精准Levy跃迁，退化为随机变异 | ⭐⭐⭐⭐⭐ |
| Model 2 (w/o Multi-Subpop) | 去掉三子种群，退化为单一评价标准 | ⭐⭐⭐⭐⭐ |
| Model 3 (w/o Isolation Penalty) | 去掉长尾用户孤岛衰减惩罚 | ⭐⭐⭐⭐⭐ |
| Model 4 (w/o Smart Stop) | 取消边际收益主动停止，强制跑满 | ⭐⭐⭐⭐ |
| Model 5 (w/o Bayesian) | 去掉贝叶斯优化初始化 | ⭐⭐⭐⭐ |
| Model 6 (w/o Adaptive) | 去掉自适应参数调整 | ⭐⭐⭐⭐ |

### 文件结构
```
min_code/
├── comparison_algorithms/
│   ├── PSO_UAV.m
│   ├── GA_UAV.m
│   ├── GOA_UAV.m
│   ├── cSA_UAV.m
│   ├── GWO_UAV.m 或 WOA_UAV.m
│   ├── NSGA_II_UAV.m
│   └── MOEA_D_UAV.m
├── ablation/
│   ├── model0_proposed.m
│   ├── model1_no_levy.m
│   ├── model2_no_subpop.m
│   ├── model3_no_penalty.m
│   ├── model4_no_stop.m
│   ├── model5_no_bayesian.m
│   └── model6_no_adaptive.m
```

### 预期成果
- 5个对比算法完整实现（PSO, GA, GOA, cSA, GWO/WOA）
- 7个消融变体完整实现
- 所有算法使用相同的 FES_max、种群大小等参数

---

## 🎯 任务4：实验环境与测试设计

### 核心底座：严格的变量控制

#### 4.1.1 统一的最大适应度评估次数（Max FES）
```
标准做法：所有算法的 Max_FES = 12000（如 种群40 × 300代）
停止条件：FES >= Max_FES 或触发提前停止（Smart Early Stop）
```

#### 4.1.2 场景一致性（固定随机种子池）
```
生成 5~10 个具有不同特征的灾后地图：
- Map1: 均匀分布
- Map2: 双中心聚集
- Map3: 边缘散落分布
- Map4: 单中心密集
- Map5: 多中心分散

保存为 Map1.mat ~ Map5.mat，所有算法必须加载这些固定地图进行测试
```

#### 4.1.3 多次运行与统计检验## 🎯 任务3：对比算法实现

### 任务目标
实现以下对比算法：

| 算法类别 | 推荐算法 | 优先级 |
|---------|---------|--------|
| 经典老牌基准 | PSO (粒子群优化) | ⭐⭐⭐⭐⭐ |
| | GA (遗传算法) | ⭐⭐⭐⭐⭐ |
| 基线算法（必须） | GOA (塘鹅优化算法) | ⭐⭐⭐⭐⭐ |
| | cSA (乌鸦搜索算法) | ⭐⭐⭐⭐⭐ |
| SOTA算法 | GWO (灰狼优化) 或 WOA (鲸鱼优化) | ⭐⭐⭐⭐ |
| 多目标专用 | NSGA-II | ⭐⭐⭐⭐ |
| | MOEA/D | ⭐⭐⭐ |
```
每个算法在每个地图上独立运行 30 次（设不同的随机种子）
记录：Best、Worst、Mean、Std
统计检验：Wilcoxon秩和检验，p < 0.05 表示显著优秀
```

### 场景设置

| 场景 | 用户数 | UAV数 | 地图类型 |
|------|--------|-------|----------|
| Small | 200 | 8 | Map1 ~ Map5 |
| Medium | 500 | 15 | Map1 ~ Map5 |
| Large | 1000 | 25 | Map1 ~ Map5 |

### 预期成果
- 固定测试地图库（Map1.mat ~ Map5.mat）
- 统一的实验参数配置
- 30次运行的完整统计数据

---

## 🎯 任务5：运行对比实验与消融实验

### 实验5.1：对比算法实验
```
对比算法：cSA-GOA, PSO, GA, GOA, cSA, GWO/WOA
场景：Small, Medium, Large × Map1 ~ Map5
运行次数：30次/算法/场景
记录指标：HV、IGD、Spread、高优先级覆盖率、全局覆盖率、单位覆盖能耗、收敛FES
```

### 实验5.2：消融实验
```
变体：Model 0 ~ Model 6
场景：Medium 和 Large（500用户、1000用户）
运行次数：30次/变体/场景
验证预期：
- Model 1: 失去Levy机制后，覆盖率阶梯突破消失
- Model 2: 失去子种群后，Spread暴跌
- Model 3: 失去惩罚后，总耗电量飙升
- Model 4: 性能与Model 0相近，但计算时间大幅增加
```

### 执行步骤
1. [x] 创建 `experiments/` 子文件夹
2. [x] 实现 `run_comparison.m` - 对比实验主程序
3. [x] 实现 `run_ablation.m` - 消融实验主程序
4. [ ] 生成固定测试地图库
5. [ ] 运行所有实验
6. [ ] 保存实验结果到 `results/` 文件夹

### 文件结构
```
min_code/
├── maps/
│   ├── Map1.mat  (均匀分布)
│   ├── Map2.mat  (双中心聚集)
│   ├── Map3.mat  (边缘散落)
│   ├── Map4.mat  (单中心密集)
│   └── Map5.mat  (多中心分散)
├── experiments/
│   ├── run_comparison.m
│   └── run_ablation.m
├── results/
│   ├── comparison/
│   │   ├── Small/
│   │   ├── Medium/
│   │   └── Large/
│   └── ablation/
```

### 预期成果
- 所有实验完整运行
- 实验结果保存为 .mat 文件
- 生成实验统计数据（均值、标准差、p-value）

---

## 🚀 实验执行命令

### 1. 消融实验

```matlab
cd d:\研究生文件合集\code\Code_main\min_code\experiments

% 默认设置 (3次运行, 500用户, 15无人机) - 快速验证
results = run_ablation();

% 论文标准实验 (30次运行)
results = run_ablation('n_runs', 30, 'n_users', 500, 'n_uavs', 15);

% 大规模测试 (1000用户, 25无人机)
results = run_ablation('n_runs', 30, 'n_users', 1000, 'n_uavs', 25);

% 单独运行某个变体
[best_fit, bestUAV, cg_curve, energy_consumption, pareto_archive] = model0_proposed(500, User, 10, RRH, RRH_type, 15, UAV_type, [1000 1000], [0 0], params, priorities);
```

### 2. 对比实验

```matlab
cd d:\研究生文件合集\code\Code_main\min_code\experiments

% 默认设置 (3次运行, 500用户, 15无人机) - 快速验证
results = run_comparison();

% 论文标准实验 (30次运行)
results = run_comparison('n_runs', 30, 'n_users', 500, 'n_uavs', 15);

% 大规模测试 (1000用户, 25无人机)
results = run_comparison('n_runs', 30, 'n_users', 1000, 'n_uavs', 25);
```

### 3. 不同场景执行

| 场景 | 用户数 | UAV数 | 执行命令 |
|------|--------|-------|----------|
| 快速验证 | 100 | 10 | `run_ablation('n_runs', 1, 'n_users', 100, 'n_uavs', 10)` |
| Small | 200 | 8 | `run_ablation('n_runs', 30, 'n_users', 200, 'n_uavs', 8)` |
| Medium | 500 | 15 | `run_ablation('n_runs', 30, 'n_users', 500, 'n_uavs', 15)` |
| Large | 1000 | 25 | `run_ablation('n_runs', 30, 'n_users', 1000, 'n_uavs', 25)` |

### 4. 批量运行脚本示例

```matlab
% run_all_experiments.m - 批量运行所有实验
cd d:\研究生文件合集\code\Code_main\min_code\experiments

scenarios = {
    'Small', 200, 8;
    'Medium', 500, 15;
    'Large', 1000, 25
};

for i = 1:size(scenarios, 1)
    scenario_name = scenarios{i, 1};
    n_users = scenarios{i, 2};
    n_uavs = scenarios{i, 3};
    
    fprintf('========== 运行场景: %s ==========\n', scenario_name);
    
    % 消融实验
    ablation_results = run_ablation('n_runs', 30, 'n_users', n_users, 'n_uavs', n_uavs);
    
    % 对比实验
    comparison_results = run_comparison('n_runs', 30, 'n_users', n_users, 'n_uavs', n_uavs);
    
    % 保存结果
    save(sprintf('../results/ablation_%s.mat', scenario_name), 'ablation_results');
    save(sprintf('../results/comparison_%s.mat', scenario_name), 'comparison_results');
end
```

### 5. Git 分支管理

```bash
cd d:\研究生文件合集\code\Code_main\min_code

% 查看当前分支
git branch

% 切换到实验分支
git checkout experiment

% 查看实验分支提交记录
git log --oneline -5

% 提交新的实验更改
git add .
git commit -m "feat: 描述实验内容"
```

---

## 🎯 任务6：结果可视化

### 任务目标
实现以下可视化图表：

| 图表类型 | 用途 | 位置 |
|---------|------|------|
| Pareto前沿对比图 | 所有算法的Pareto前沿叠加 | 主文 |
| HV收敛曲线 | 展示收敛速度 | 主文 |
| IGD收敛曲线 | 展示逼近真实前沿的程度 | 主文/附录 |
| Spread对比图 | 分布性对比 | 主文/附录 |
| 消融实验对比图 | 验证各模块有效性 | 主文/附录 |
| 箱型图 (Boxplot) | 30次运行的鲁棒性展示 | 主文 |
| 统计检验表 | Wilcoxon p-value | 主文 |

### 执行步骤
1. [ ] 创建 `visualization/` 子文件夹
2. [ ] 实现 `plot_pareto_comparison.m` - Pareto前沿对比
3. [ ] 实现 `plot_convergence_hv.m` - HV收敛曲线
4. [ ] 实现 `plot_convergence_igd.m` - IGD收敛曲线
5. [ ] 实现 `plot_metric_bar.m` - 指标柱状图
6. [ ] 实现 `plot_ablation_results.m` - 消融实验结果
7. [ ] 实现 `plot_boxplot.m` - 箱型图
8. [ ] 实现 `plot_statistical_tests.m` - 统计检验结果
9. [ ] 创建 `generate_all_plots.m` - 一键生成所有图表

### 文件结构
```
min_code/
├── visualization/
│   ├── plot_pareto_comparison.m
│   ├── plot_convergence_hv.m
│   ├── plot_convergence_igd.m
│   ├── plot_metric_bar.m
│   ├── plot_ablation_results.m
│   ├── plot_boxplot.m
│   ├── plot_statistical_tests.m
│   └── generate_all_plots.m
├── figures/
│   ├── comparison/
│   └── ablation/
```

### 预期成果
- 所有可视化函数完整实现
- 生成高质量的论文图表（PDF/SVG格式）
- 图表有清晰的标题、图例、坐标轴标签

---

## 🎯 任务7：论文实验章节结构

### 推荐章节结构

```
4. 实验与分析

4.1 实验环境与参数设置
    4.1.1 统一FES控制与参数设置
    4.1.2 测试场景与地图库
    4.1.3 性能评价指标

4.2 消融实验分析 (Ablation Study)
    4.2.1 精准Levy跃迁机制有效性验证
         - 收敛曲线对比：阶梯突破 vs 彻底停滞
    4.2.2 孤岛惩罚机制有效性验证
         - 能耗对比柱状图
    4.2.3 多子种群协同机制有效性验证
         - Spread指标对比
    4.2.4 边际收益停止机制有效性验证
         - 性能 vs 计算时间对比
    4.2.5 贝叶斯优化与自适应调整贡献分析

4.3 综合性能对比分析 (Comparative Analysis)
    4.3.1 单目标性能对比
         - 与PSO、GA、GOA、cSA、GWO的Coverage/Energy表格
         - 加粗最优值
    4.3.2 算法鲁棒性与统计检验
         - 30次运行Boxplot箱型图
         - Wilcoxon检验p-value表
    4.3.3 多目标性能指标对比
         - HV、IGD、Spread指标表

4.4 多目标部署方案分析 (Multi-objective Insights)
    4.4.1 Pareto前沿分布对比
         - 所有算法Pareto前沿叠加散点图
         - HV和IGD计算结果
    4.4.2 典型部署拓扑展示
         - 无人机和用户二维连线图
```

---

## 📊 任务优先级和时间估算

| 任务 | 优先级 | 预计时间 | 依赖任务 | 状态 |
|------|--------|---------|---------|------|
| 任务1：整理现有代码 | ⭐⭐⭐⭐⭐ | 1-2天 | 无 | ✅ 完成 |
| 任务2：性能指标 | ⭐⭐⭐⭐⭐ | 2-3天 | 任务1 | ✅ 完成 |
| 任务3：对比算法 | ⭐⭐⭐⭐⭐ | 5-7天 | 任务1, 2 | 🔄 进行中 |
| 任务4：实验环境设计 | ⭐⭐⭐⭐⭐ | 2-3天 | 任务1 | 🔄 进行中 |
| 任务5：运行实验 | ⭐⭐⭐⭐⭐ | 7-14天 | 任务3, 4 | ⏳ 待开始 |
| 任务6：结果可视化 | ⭐⭐⭐⭐ | 3-5天 | 任务5 | ⏳ 待开始 |
| 任务7：论文结构 | ⭐⭐⭐ | 1-2天 | 任务5, 6 | ⏳ 待开始 |

**总计：约 3-4 周**

---

## 📝 注意事项

1. **变量控制**：所有算法必须使用相同的 Max_FES = 12000
2. **地图固定化**：提前生成 5 个标准地图，所有算法在相同地图上测试
3. **统计检验**：使用 Wilcoxon 秩和检验，p < 0.05 表示显著优秀
4. **版本控制**：使用 Git 管理代码，experiment 分支用于实验
5. **实验备份**：定期备份 `results/` 文件夹
6. **并行计算**：使用 MATLAB Parallel Computing Toolbox 加速实验

---

## 🎉 完成标志

当以下条件都满足时，任务完成：
- ✅ 所有对比算法和消融变体完整实现
- ✅ 固定测试地图库生成完毕
- ✅ 所有实验完整运行（30次/算法/场景），结果保存
- ✅ 所有论文图表生成完毕
- ✅ 代码可以复现所有结果
- ✅ 有清晰的 README 说明如何运行代码

祝论文发表顺利！🚀
